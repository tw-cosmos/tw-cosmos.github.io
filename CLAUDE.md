# CLAUDE.md

## 中文標點規則

本規則適用於本 repo 的中文內容：`data/*.yml`、生成頁面、`README.md`。
不適用於 commit message 與程式註解（兩者維持英文）。

1. 中文標點一律全形：，。、；：？！（）「」
2. 全形標點前後不加空格，一律貼緊
3. 引號用全形「」，不使用半形 " "
4. 括號用全形（），不使用半形 ( )
5. 例外：英文、數字、時間、金額等半形內容與中文之間，
   保留一個半形空格（例：Cosmos CLUB 會員、每日限量 3 間、15:00 - 17:00）
5-1. 例外：URL 後方緊接全形標點時，保留一個半形空格作為網址結束邊界。
   此空格為功能性分隔，優先於第 2 條的貼緊規則。
   原因：無此空格時，連結自動偵測會將標點併入網址。
6. 半形逗號僅用於千分位（NT$3,045）
7. 不使用分隔號 ・，並列名詞改用全形頓號 、

## Windows 環境注意

終端機預設 cp950 編碼，顯示含中文的檔案內容時請用：

```python
sys.stdout.buffer.write(content.encode("utf-8"))
```

或執行前先 `chcp 65001`。
