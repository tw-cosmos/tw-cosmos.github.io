# snapshots

Stored copies of the visible text of the source pages listed in
`scripts/sources.yml`.

These files exist only so that the weekly check has something to compare
against. They are written by the automated workflow and are not edited by
hand, and they are not used to build any page.

When the weekly check finds a difference it opens an issue with the diff
and updates the snapshot here.
